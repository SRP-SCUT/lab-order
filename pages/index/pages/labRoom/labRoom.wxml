<wux-calendar id="wux-calendar"/>
<view class="page">
    <view class="page__bd">
        <wux-grids square>
          <block wx:for-items="{{labRoom}}" wx:key="{{item.id}}">
            <wux-grid thumb="../../pages/index/imgs/labRoom.png" label="{{item.id}}" hoverClass="{{item.ordered? none : default}}" bind:click="toOrder"/>
          </block>
        </wux-grids>
    </view>
</view>